#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int t;
    cin >> t;
    while (t--)
    {
        int x, y;
        char op;
        cin >> x >> op >> y;
        cout << (x + y) << '\n';
    }
    return 0;
}